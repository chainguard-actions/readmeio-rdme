name: Test rdme openapi validate (invalid spec)
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Create dist-gha/package.json (equivalent to pre step write-gha-pjson.js)
        run: |
          # The action's pre step (bin/write-gha-pjson.js) creates dist-gha/package.json
          # but pre steps don't run for local actions. We replicate it here using jq.
          jq '
            .private = true |
            .oclif.commands.target = "./commands.js" |
            .oclif.hooks |= with_entries(.value.target = "./commands.js") |
            del(.oclif.helpClass) |
            del(.oclif.plugins)
          ' package.json > dist-gha/package.json
          echo "Created dist-gha/package.json"
      - id: action
        uses: .
        continue-on-error: true
        with:
          rdme: openapi validate ${{ github.workspace }}/tests/fixtures/invalid-spec.json
      - name: Assert action failed on invalid spec
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail on invalid spec, but got outcome: $OUTCOME"
            exit 1
          fi
          echo "PASS: action correctly failed on invalid spec"
