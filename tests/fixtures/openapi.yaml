openapi: "3.0.3"
info:
  title: Test API
  version: "1.0.0"
  description: A minimal OpenAPI spec for testing rdme validate
paths:
  /hello:
    get:
      summary: Say hello
      operationId: sayHello
      responses:
        "200":
          description: A successful response
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Hello, world!
