openapi: "3.0.3"
info:
  title: Test API
  version: "1.0.0"
paths:
  /hello:
    get:
      summary: Say hello
      responses:
        "200":
          description: A greeting
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
